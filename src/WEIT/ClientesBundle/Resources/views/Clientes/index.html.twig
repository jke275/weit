{% extends 'layout.html.twig' %}

{% block body %}
	{{ parent() }}
	{{ include('WEITPersonalBundle:Personal:mensajes/success.html.twig') }}
	<div class="container">
		<dv class="col-md-12">
			<div class="page-header marggin-none">
				<h2 class="padding-none">Clientes</h2>
			</div>
			<div class="table-responsive">
				<table class="table table-striped table-hover">
					<thead>
						<tr>
							<th>Nombre</th>
							<th>Apellidos</th>
							<th>Correo</th>
							<th>Teléfono</th>
						</tr>
					</thead>
					<tbody>
						{% for user in personal %}
							<tr>
								<td>{{ user.nombre|capitalize }}</td>
								<td>{{ user.apellidos|capitalize }}</td>
								<td>{{ user.correo }}</td>
								<td>{{ user.telefono|capitalize }}</td>
								<td class="actions">
									<a href="{{ path('weit_clientes_editar', {id: user.id}) }}" class="btn btn-sm btn-info">Editar</a>
									<a href="{{ path('weit_clientes_ver', {id: user.id}) }}" class="btn btn-sm btn-primary">Ver</a>
									<a href="{{ path('weit_espectaculares_disponibles', {cliente: user.id}) }}" class="btn btn-sm btn-primary">Vender</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</dv>
	</div>

{% endblock %}
